#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

if [ -z "${DISPLAY}" ] && [ "${XDG_VTNR}" -eq 2 ]; then
  exec startx
fi

# Added by LM Studio CLI (lms)
export PATH="$PATH:/home/manuel/.lmstudio/bin"
