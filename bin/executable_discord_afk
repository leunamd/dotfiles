#!/bin/bash
timestamp() {
    date "+%Y-%m-%d %H:%M:%S"
}

if [ -f /tmp/discord-afk.pid ]; then
    echo "discord_afk $(timestamp): AFK triggered"
    kill -USR1 $(cat /tmp/discord-afk.pid)
else
    echo "discord_afk $(timestamp): daemon not running"
fi
